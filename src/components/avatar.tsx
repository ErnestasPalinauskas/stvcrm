import React from 'react';
import Menu from './menu';

const Avatar: React.FC = () => {
    return (
        <div style={{ width: '100%', backgroundColor: 'yellow', height: '200px' }}>
            <Menu />
        </div>
    );
};

export default Avatar;
